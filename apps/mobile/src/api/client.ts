// In Expo Go (__DEV__ = true) hit your local API so you use the dev DB branch.
// Replace the IP below with your machine's local IP (run `ipconfig` on Windows,
// look for IPv4 under your Wi-Fi adapter — e.g. 192.168.1.42).
// Your phone and laptop must be on the same Wi-Fi network.
export const API_URL = __DEV__
  ? "http://YOUR_LOCAL_IP:4000"
  : "https://new-me-l46q.onrender.com";
