# PostgreSQL connection string — only needed here for running Prisma CLI commands locally
# (prisma migrate dev, prisma studio, etc.)
# Use your Neon dev branch connection string, not production
DATABASE_URL=
